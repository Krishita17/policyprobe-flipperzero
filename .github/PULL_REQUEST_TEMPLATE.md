## What this changes

<!-- If this touches mappings/protocol_weakness_control.yaml, say which control mappings
     changed and why. The mapping is the contribution; changes to it need a rationale. -->

## Checklist

- [ ] `make validate` passes (no dangling control references)
- [ ] `make test` passes
- [ ] If the knowledge base changed, `make tables` was run so the README matches it
- [ ] Any new mapping row carries a rationale that an auditor could read
